function length object
    set size to 0
    set achouFim to 0

    while 1

        if achouFim = 1
            set size to size-1
            break

        check achouFim
            if size@object
                nothing       

        set size to size + 1
        
    result size